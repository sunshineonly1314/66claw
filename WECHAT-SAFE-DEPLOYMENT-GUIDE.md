# 微信自动化客服安全部署指南

> ⚠️ **重要警告**: 本指南仅用于合法的个人助理场景，严禁用于骚扰、广告、诈骗等违法用途。

## 📊 测试结果总结

✅ **防检测机制已完整实现并测试通过**

### 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 随机延迟回复 | ✅ | 2-8秒基础延迟 + 打字模拟延迟 |
| 限流控制 | ✅ | 每小时≤30条，每天≤100条 |
| 黑名单过滤 | ✅ | 自动拒绝包含敏感词的消息 |
| 夜间静默 | ✅ | 00:00-07:00 不自动回复 |
| 白名单控制 | ✅ | 可限制仅回复指定用户 |
| 打字延迟模拟 | ✅ | 根据消息长度动态调整 |

### 测试数据

```
消息长度 → 延迟时间
  10字   →  2.5s - 9.1s  (平均 5.5s)
  50字   →  2.8s - 10.7s (平均 6.2s)
 100字   →  4.5s - 9.9s  (平均 7.6s)
 200字   →  6.1s - 11.5s (平均 8.8s)
 500字   → 10.3s - 21.1s (平均 14.9s)
```

---

## 🚀 快速部署 (5步完成)

### 步骤 1: 获取 ClawChat API Key

```bash
# 1. 微信搜索 "ClawChat" 小程序
# 2. 微信授权登录（首次自动注册）
# 3. 点击 "我的" → "APIKey 管理"
# 4. 点击 "生成 APIKey"
# 5. 复制 API Key (格式: 12345:abcdef1234567890)
```

### 步骤 2: 获取通义千问 API Key (推荐)

```bash
# 1. 访问 https://dashscope.console.aliyun.com/
# 2. 登录/注册阿里云账号
# 3. 开通 DashScope 服务（免费额度足够测试）
# 4. 创建 API Key
# 5. 复制保存
```

### 步骤 3: 创建配置文件

```bash
# 复制测试配置模板
cp test-wechat-safe.json5 ~/.openclawcn/config.json5

# 编辑配置文件
nano ~/.openclawcn/config.json5
# 或 Windows: notepad %USERPROFILE%\.openclawcn\config.json5
```

**必须修改的字段：**

```json5
{
  models: {
    providers: {
      "qwen-dashscope": {
        // 👇 替换这里
        apiKey: "sk-你的通义千问API Key",
      }
    }
  },

  plugins: {
    entries: {
      openclawwechat: {
        config: {
          // 👇 替换这里
          apiKey: "你的ClawChat API Key (格式: 12345:abcdef...)",
        }
      }
    }
  }
}
```

### 步骤 4: 启动服务

```bash
# 安装依赖 (如果还没有)
npm install

# 启动网关
npm run gateway

# 或者直接运行
node dist/gateway/server.js
```

### 步骤 5: 测试

```bash
# 用另一个微信号给你的客服号发消息:
"你好" → 应该在 2-8 秒后收到回复

# 测试黑名单:
"这是测试消息" → 不会回复 (包含"测试"关键词)

# 查看日志:
tail -f ~/.openclawcn/logs/gateway.log
```

---

## 🔒 防封号核心策略

### ✅ 安全做法

1. **只回复用户主动发来的消息**
   - ✅ 用户: "你好" → 你: "你好，有什么可以帮到你？"
   - ❌ 主动群发: "Hi，我是XX客服"

2. **回复要像真人**
   ```
   ❌ 不好的回复:
   "您好！我是智能客服机器人，很高兴为您服务！请问有什么可以帮助您的吗？"

   ✅ 好的回复:
   "在的，有什么事吗？😊"
   ```

3. **遇到敏感话题立即转人工**
   ```
   敏感话题包括:
   - 投诉、纠纷
   - 金钱交易
   - 法律问题
   - 医疗建议

   回复模板:
   "这个问题比较重要，我让主人稍后回你哈"
   ```

4. **严格限流**
   ```json5
   {
     antiDetection: {
       maxRepliesPerHour: 30,   // 每小时最多30条
       maxRepliesPerDay: 100,   // 每天最多100条
       replyDelayMin: 2,        // 最少延迟2秒
       replyDelayMax: 8,        // 最多延迟8秒
     }
   }
   ```

5. **夜间静默**
   ```json5
   {
     antiDetection: {
       quietHours: {
         start: "00:00",  // 凌晨0点
         end: "07:00"     // 早上7点
       }
     }
   }
   ```

### ❌ 危险行为 (会被封号)

1. ❌ 主动群发广告
2. ❌ 每秒回复多条消息
3. ❌ 回复内容包含"机器人"、"自动回复"
4. ❌ 回复模板化、千篇一律
5. ❌ 发送违规内容 (色情、赌博、诈骗)
6. ❌ 频繁切换IP地址
7. ❌ 使用多个微信号同时自动化

---

## 📝 配置详解

### 防检测配置参数

```json5
{
  plugins: {
    entries: {
      openclawwechat: {
        config: {
          // ===== 基础配置 =====
          apiKey: "your-api-key",        // ClawChat API Key
          pollIntervalMs: 3000,          // 轮询间隔 (毫秒)
          sessionKey: "agent:bot:main",  // 会话标识
          debug: true,                   // 调试日志

          // ===== 防检测配置 =====
          antiDetection: {
            // 回复延迟范围 (秒)
            replyDelayMin: 2,
            replyDelayMax: 8,

            // 限流配置
            maxRepliesPerHour: 30,    // 每小时最多回复数
            maxRepliesPerDay: 100,    // 每天最多回复数

            // 打字延迟 (每100字符延迟秒数)
            typingDelayPer100Chars: 2,

            // 夜间静默时段
            quietHours: {
              start: "00:00",
              end: "07:00"
            },

            // 黑名单关键词 (包含这些词的消息不回复)
            blacklistKeywords: [
              "测试",
              "test",
              "机器人",
              "bot",
              "自动回复"
            ],

            // 白名单用户 (只回复这些用户，为空则回复所有)
            whitelistUsers: [
              // "openid_1",
              // "openid_2"
            ]
          }
        }
      }
    }
  }
}
```

### Agent 配置 (人格设定)

```json5
{
  agent: {
    model: "qwen-max",
    temperature: 0.7,      // 提高随机性，避免机械化
    maxTokens: 300,        // 限制回复长度

    systemPrompt: `你是一个友好的个人助手（不是客服机器人）。

【身份设定】
- 你是微信号主人的朋友，帮忙回复消息
- 不要说你是"机器人"、"AI助手"、"客服"
- 语气要像真人聊天，自然、随意

【回复规则】
1. 简短回复（不超过50字）
2. 使用口语化表达，可以用"哈哈"、"嗯嗯"
3. 偶尔加入表情符号 😊
4. 不要每句话都很正式

【场景处理】
- "在吗" → "在的，咋了？"
- 广告 → "不好意思，不需要" 或不回复
- 私人问题 → "这个我不太清楚，稍后让TA回你"

【重要】
- 绝对不要透露你是AI
- 遇到不确定的，说"我问一下"
- 避免"根据您的需求"等客服话术

记住：你是在帮朋友回微信，不是做客服。`
  }
}
```

---

## 📈 监控和维护

### 查看运行状态

```bash
# 查看统计信息
curl http://localhost:18789/api/stats

# 查看日志
tail -f ~/.openclawcn/logs/gateway.log

# 查看限流状态
grep "超过" ~/.openclawcn/logs/gateway.log
```

### 日志示例

```
[2024-02-18 14:30:25] ✅ 允许回复: openid=oABC123, text="你好", delay=3.2s
[2024-02-18 14:30:28] 📤 已发送回复: openid=oABC123
[2024-02-18 14:31:10] ❌ 拒绝回复: openid=oDEF456, reason=包含黑名单关键词
[2024-02-18 14:45:00] ⏸️  夜间静默: openid=oGHI789
[2024-02-18 15:00:00] 🚫 限流触发: 本小时已回复30条
```

### 调整策略

根据实际使用情况调整参数：

```json5
// 如果觉得回复太慢:
{
  replyDelayMin: 1,
  replyDelayMax: 5,
}

// 如果消息量大:
{
  maxRepliesPerHour: 50,
  maxRepliesPerDay: 200,
}

// 如果不想夜间静默:
{
  quietHours: {
    start: "00:00",
    end: "00:00"  // 同时设为00:00表示禁用
  }
}
```

---

## 🧪 测试清单

### 功能测试

- [ ] 正常消息能收到回复
- [ ] 回复延迟符合预期 (2-8秒)
- [ ] 黑名单关键词被正确拦截
- [ ] 夜间时段不自动回复
- [ ] 超过限流后停止回复
- [ ] 白名单功能正常

### 安全测试

- [ ] 回复内容不包含"机器人"、"AI"等敏感词
- [ ] 回复语气自然，像真人
- [ ] 不会主动发送消息
- [ ] 遇到敏感话题能正确转人工

### 压力测试

```bash
# 快速发送10条消息，检查限流
for i in {1..10}; do
  echo "发送消息 $i"
  # 用微信发送消息
  sleep 1
done

# 预期结果: 前5-8条有回复，后续被限流拦截
```

---

## ⚠️ 风险提示

### 微信封号风险评估

| 使用场景 | 风险等级 | 说明 |
|---------|---------|------|
| 个人助理，偶尔回复朋友 | 🟢 低 | 符合正常使用 |
| 小规模客服 (<20人/天) | 🟡 中 | 需严格限流 |
| 大规模客服 (>50人/天) | 🔴 高 | 不推荐 |
| 主动群发广告 | 🔴 极高 | 必封号 |

### 降低风险的建议

1. **渐进式测试**
   ```
   第1周: 每天回复10条，观察
   第2周: 每天回复20条
   第3周: 每天回复30条
   稳定2周后再考虑增加
   ```

2. **准备备用方案**
   - 备用微信号 (一旦被封可切换)
   - 人工介入机制 (重要客户手动回复)
   - 其他渠道 (企业微信、钉钉等)

3. **定期检查**
   ```bash
   # 每天检查限流统计
   grep "限流触发" logs/gateway.log | wc -l

   # 每周检查回复质量
   # 人工抽查10条回复，确保自然
   ```

---

## 🆘 故障排查

### 问题: 收不到消息

```bash
# 1. 检查 ClawChat 绑定状态
# 打开 ClawChat 小程序 → 我的 → 查看绑定状态

# 2. 检查 API Key 是否正确
grep "apiKey" ~/.openclawcn/config.json5

# 3. 检查网络连接
curl https://api.clawchat.mifengcdn.com

# 4. 查看日志
tail -f ~/.openclawcn/logs/gateway.log | grep "Polling"
```

### 问题: 不回复消息

```bash
# 检查是否被限流
tail logs/gateway.log | grep "拒绝回复"

# 检查是否在夜间静默时段
date  # 查看当前时间

# 检查黑名单关键词
grep "blacklistKeywords" config.json5
```

### 问题: 回复太机械

修改 systemPrompt，加入更多人性化元素：

```json5
systemPrompt: `你是我的朋友，性格开朗、幽默。

回复风格:
- 多用"哈哈"、"嗯嗯"、"好呀"
- 偶尔用表情 😊 😂 👍
- 不要太正式，像聊天一样

例子:
- "在吗" → "在呢，咋啦？"
- "谢谢" → "不客气哈哈"
- "晚安" → "晚安～明天见 😴"
`
```

---

## 📞 支持

- **GitHub Issues**: [提交问题](https://github.com/anthropics/clawdbot/issues)
- **文档**: [完整文档](./docs/channels/openclawwechat-setup.md)

---

## 📄 免责声明

本项目仅供学习和合法用途。使用者需：

1. 遵守微信用户协议和相关法律法规
2. 不得用于骚扰、诈骗、传播违法信息
3. 自行承担因使用本项目导致的账号风险

**作者不对因使用本项目导致的账号封禁、法律纠纷等后果承担责任。**
