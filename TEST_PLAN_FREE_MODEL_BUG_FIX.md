# 免费模型Bug修复测试计划

## 测试目标
验证6个关键Bug的修复是否有效，确保免费模型切换逻辑能正确处理401错误，并在所有免费模型额度耗尽后平滑回退到付费模型。

## 修复的Bug列表
1. **Bug #1**: `detectQuotaExhaustedError`不检查401状态码
2. **Bug #2**: `usingFreeModel`状态变量不同步
3. **Bug #3**: 错误消息"401 status code (no body)"不匹配关键字
4. **Bug #4**: `cfg`配置对象污染
5. **Bug #5**: `detectQuotaExhaustedError`调用缺少`httpStatus`参数
6. **Bug #6**: `agent-runner-execution.ts`中`meta.error`的401错误被静默忽略

---

## 测试用例

### **TC-001: 401错误检测（Bug #1, #3, #5修复验证）**
**前置条件**:
- 配置至少一个免费模型账户
- 免费模型API返回401状态码

**测试步骤**:
1. 发送消息触发免费模型调用
2. 模拟API返回 `{ status: 401, message: "401 status code (no body)" }`

**预期结果**:
- ✅ `detectQuotaExhaustedError`返回`true`
- ✅ 系统尝试切换到下一个免费模型
- ✅ 日志显示 `[FreeModel] 检测到额度用尽错误 (free-model-xxx, httpStatus=401)`

**实际结果**: ⏳ 待测试

---

### **TC-002: 401+关键字检测（Bug #1修复验证）**
**前置条件**: 同TC-001

**测试步骤**:
1. 发送消息触发免费模型调用
2. 模拟API返回 `{ status: 401, message: "quota exceeded" }`

**预期结果**:
- ✅ `detectQuotaExhaustedError`返回`true`（匹配关键字"quota"）
- ✅ 系统尝试切换到下一个免费模型

**实际结果**: ⏳ 待测试

---

### **TC-003: 401但非quota错误（Bug #1负面测试）**
**前置条件**: 同TC-001

**测试步骤**:
1. 发送消息触发免费模型调用
2. 模拟API返回 `{ status: 401, message: "invalid api key" }`

**预期结果**:
- ✅ `detectQuotaExhaustedError`返回`false`（没有quota关键字）
- ✅ 错误直接抛出，不尝试切换模型
- ✅ 用户看到认证错误提示

**实际结果**: ⏳ 待测试

---

### **TC-004: meta.error中的401错误（Bug #6修复验证）**
**前置条件**: 同TC-001

**测试步骤**:
1. 发送消息触发免费模型调用
2. Pi SDK返回 `runResult.meta.error = { message: "401 status code (no body)", httpStatus: 401 }`（不抛出异常）

**预期结果**:
- ✅ `agent-runner-execution.ts`检测到`meta.error`中的quota错误
- ✅ 抛出异常，让上层免费模型切换逻辑处理
- ✅ 日志显示 `[FreeModel] Detected quota error in meta.error`
- ✅ 系统尝试切换到下一个免费模型

**实际结果**: ⏳ 待测试

---

### **TC-005: 状态同步（Bug #2修复验证）**
**前置条件**:
- 配置2个免费模型账户
- 第1个账户额度耗尽

**测试步骤**:
1. 发送消息，触发第1个免费模型调用
2. 第1个模型返回401错误
3. 系统切换到第2个免费模型
4. 第2个模型也返回401错误
5. 所有免费模型耗尽，回退到付费模型

**预期结果**:
- ✅ 在切换到第2个模型时，`usingFreeModel`状态为`true`
- ✅ 回退到付费模型后，`usingFreeModel`状态为`false`
- ✅ 循环结束时，`reply === undefined`且`usingFreeModel === false`
- ✅ 不会错误地返回"模型响应失败"提示（因为最终使用的是付费模型）

**实际结果**: ⏳ 待测试

---

### **TC-006: 配置隔离（Bug #4修复验证）**
**前置条件**: 同TC-005

**测试步骤**:
1. 发送消息，触发免费模型调用
2. 免费模型调用失败，所有免费模型耗尽
3. 系统回退到付费模型

**预期结果**:
- ✅ 回退到付费模型时，使用的是`originalCfg`，不是被修改过的`cfg`
- ✅ 付费模型的配置（如baseUrl, apiKey）没有被污染
- ✅ 付费模型调用成功

**实际结果**: ⏳ 待测试

---

### **TC-007: 完整流程（所有Bug综合验证）**
**前置条件**:
- 配置3个免费模型账户，全部额度耗尽
- 配置1个付费模型账户

**测试步骤**:
1. 发送消息 "你好"
2. 系统尝试使用第1个免费模型 → 返回401
3. 系统切换到第2个免费模型 → 返回401
4. 系统切换到第3个免费模型 → 返回401
5. 所有免费模型耗尽，回退到付费模型
6. 付费模型成功返回响应

**预期结果**:
- ✅ 每次401错误都被正确检测（Bug #1, #5修复）
- ✅ meta.error中的401也被捕获（Bug #6修复）
- ✅ 状态正确同步（Bug #2修复）
- ✅ 配置不被污染（Bug #4修复）
- ✅ 用户最终收到付费模型的正常回复
- ✅ 用户看到通知："ClawdbotCN专属权益：今日免费额度已全部用完，已自动切换回付费模型"

**实际结果**: ⏳ 待测试

---

### **TC-008: 用户无输出问题（核心场景）**
**前置条件**: 同TC-007

**测试步骤**:
1. 用户发送消息 "chat"
2. 观察系统行为和用户收到的响应

**预期结果**:
- ✅ 用户**必须**收到响应（要么是付费模型的回复，要么是错误提示）
- ✅ **不能**出现"没有任何输出"的情况
- ✅ Gateway不崩溃

**实际结果**: ⏳ 待测试

---

## 测试环境
- Node.js 22+
- Clawdbot latest (修复后版本)
- 测试用免费API账户（可控制返回状态码）

## 测试执行计划
1. **单元测试**: 针对`detectQuotaExhaustedError`函数
2. **集成测试**: 完整的免费模型切换流程
3. **端到端测试**: 从用户发送消息到收到回复的完整链路

## 风险评估
- ⚠️ **高风险**: 如果Bug #6未修复，meta.error中的401仍会被忽略
- ⚠️ **中风险**: 如果状态同步有问题，可能导致错误的fallback判断
- ⚠️ **低风险**: 配置污染可能在特定场景下导致付费模型调用失败

## 回归测试
- ✅ 确认修复没有破坏现有的402/429错误检测
- ✅ 确认非免费模型用户不受影响
- ✅ 确认单个免费模型账户的正常使用场景

---

## 测试结论
⏳ **待执行**

所有测试用例执行完毕后，更新此处结论。
