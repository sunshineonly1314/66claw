# Batch 10: Session 维护系统 (CORE-08)

> 双人审查制 | 开始时间: 2026-02-11 22:50
> **优先级**: P1 功能 | **规模**: 23 文件/1566 行 | **类型**: 大型新功能

---

## 问题描述

**上游 commit**: 需查找 (CORE-08 Session maintenance system)

**功能**: 自动清理过期 session、管理 session 生命周期、防止存储无限增长

**CN 现状**: 待分析

---

## 实施者1号 — 初步分析

### 第一步：搜索 CN 现有 session 管理

让我找到所有与 session 维护相关的代码：

**CN 现状搜索结果**:
- ❌ 无 `cleanupSessions`、`expireSession`、`session maintenance` 相关代码
- ✅ 有 `SessionManager` (19 个文件使用)
- ❌ `src/config/sessions/types.ts` 无 `maxAge`、`ttl`、`expire` 配置
- ❌ `src/config/sessions/store.ts` 无清理函数

**结论**: CN 没有自动 session 清理功能

---

## 实施者1号+2号 — 双人讨论

### 🗣️ 实施者1号（问题提出）

CORE-08 是一个 **23 文件/1566 行**的大型新功能。分析规模：

**上游改动预估**:
1. 新增 session TTL 配置
2. 新增后台清理任务
3. 新增 session 元数据追踪（最后访问时间）
4. 修改所有 SessionManager 调用点
5. 新增清理 API/CLI 命令
6. 新增测试文件

**问题**:
1. **规模巨大**: 23 文件是目前为止最大的单项改动
2. **风险高**: 涉及核心 session 管理，任何 bug 都可能丢失对话数据
3. **测试复杂**: 需要时间相关测试（TTL、后台任务）
4. **收益评估**: CN 是单用户自托管，session 过期的紧迫性远低于多租户 SaaS

**建议**: 是否应该延后到最后，优先完成其他更小的项目？

---

### 🗣️ 实施者2号（风险评估）

同意这是高风险项目。让我评估必要性：

**威胁/问题评估**:

| 问题 | 严重性 | CN 现状 | 解决紧迫性 |
|------|--------|---------|-----------|
| 存储无限增长 | P2 | 可能存在 | 中（月/年级别） |
| 过期 session 占用磁盘 | P3 | 可能存在 | 低 |
| 性能下降（太多文件） | P3 | 取决于使用量 | 低 |
| 数据泄露（旧 session） | P3 | 低风险 | 低 |

**CN 特性**:
- 单用户自托管
- session 存储在本地文件系统
- 用户可以手动删除 session 文件
- 没有多租户隔离需求

**对比其他延后项**:
- SEC-11 Skill Safety Scanner (7+ 文件) — **更小、更安全**
- CH-11 Discord Forum (中等规模) — **更小、P2 功能**
- 其他架构级项目 — 都比这个小

**建议**: ✅ **跳过 CORE-08，优先 SEC-11**

理由：
1. 23 文件风险太高，可能引入 session 数据丢失 bug
2. CN 单用户场景下，session 清理不紧迫
3. 用户可以手动管理 session 文件
4. SEC-11 更小（7文件）、更独立、风险更低

---

### 🗣️ 实施者1号（最终决策）

同意跳过 CORE-08。这是本次合并工作中**最大的单项功能**，风险/收益比不合理。

**替代方案** (如果未来需要):
1. **轻量级**: 添加 CLI 命令 `clawdbot sessions clean --older-than 30d`
2. **文档**: 告知用户手动清理路径
3. **配置**: 添加 `session.maxAge` 配置（仅用于将来）

**决策**: ❌ **跳过 CORE-08** — 规模太大（23文件），收益不明显（CN单用户场景）

---

## Batch 10 结论

**分析项目**: CORE-08 Session 维护系统
**决策**: ❌ 跳过
**原因**:
- 规模过大（23 文件/1566 行）
- 风险高（核心 session 管理）
- 收益低（CN 单用户自托管）
- 有更小、更安全的项目待处理

**下一步**: 转向 SEC-11 Skill Safety Scanner (7+ 文件，独立功能模块)

---

**Batch 10 完成时间**: 2026-02-11 23:00
**状态**: 跳过
