name: OpenClawCN
options:
  bundleIdPrefix: ai.openclawcn
  deploymentTarget:
    iOS: "18.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "6.0"

packages:
  OpenClawCNKit:
    path: ../shared/OpenClawCNKit
  Swabble:
    path: ../../Swabble

schemes:
  OpenClawCN:
    shared: true
    build:
      targets:
        OpenClawCN: all
    test:
      targets:
        - OpenClawCNTests

targets:
  OpenClawCN:
    type: application
    platform: iOS
    sources:
      - path: Sources
    dependencies:
      - package: OpenClawCNKit
      - package: OpenClawCNKit
        product: OpenClawCNChatUI
      - package: OpenClawCNKit
        product: OpenClawCNProtocol
      - package: Swabble
        product: SwabbleKit
      - sdk: AppIntents.framework
    preBuildScripts:
      - name: SwiftFormat (lint)
        basedOnDependencyAnalysis: false
        inputFileLists:
          - $(SRCROOT)/SwiftSources.input.xcfilelist
        script: |
          set -euo pipefail
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          if ! command -v swiftformat >/dev/null 2>&1; then
            echo "error: swiftformat not found (brew install swiftformat)" >&2
            exit 1
          fi
          swiftformat --lint --config "$SRCROOT/../../.swiftformat" \
            --filelist "$SRCROOT/SwiftSources.input.xcfilelist"
      - name: SwiftLint
        basedOnDependencyAnalysis: false
        inputFileLists:
          - $(SRCROOT)/SwiftSources.input.xcfilelist
        script: |
          set -euo pipefail
          export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
          if ! command -v swiftlint >/dev/null 2>&1; then
            echo "error: swiftlint not found (brew install swiftlint)" >&2
            exit 1
          fi
          swiftlint lint --config "$SRCROOT/.swiftlint.yml" --use-script-input-file-lists
    settings:
      base:
        CODE_SIGN_IDENTITY: "Apple Development"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: Y5PE65HELJ
        PRODUCT_BUNDLE_IDENTIFIER: ai.openclawcn.ios
        PROVISIONING_PROFILE_SPECIFIER: "ai.openclawcn.ios Development"
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        ENABLE_APPINTENTS_METADATA: NO
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: OpenClawCN
        CFBundleIconName: AppIcon
        CFBundleShortVersionString: "2026.2.15"
        CFBundleVersion: "20260215"
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIBackgroundModes:
          - audio
        NSLocalNetworkUsageDescription: OpenClawCN discovers and connects to your OpenClawCN gateway on the local network.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoadsInWebContent: true
        NSBonjourServices:
          - _openclawcncn-gw._tcp
        NSCameraUsageDescription: OpenClawCN can capture photos or short video clips when requested via the gateway.
        NSLocationWhenInUseUsageDescription: OpenClawCN uses your location when you allow location sharing.
        NSLocationAlwaysAndWhenInUseUsageDescription: OpenClawCN can share your location in the background when you enable Always.
        NSMicrophoneUsageDescription: OpenClawCN needs microphone access for voice wake.
        NSSpeechRecognitionUsageDescription: OpenClawCN uses on-device speech recognition for voice wake.
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  OpenClawCNTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    dependencies:
      - target: OpenClawCN
      - package: Swabble
        product: SwabbleKit
      - sdk: AppIntents.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ai.openclawcn.ios.tests
        SWIFT_VERSION: "6.0"
        SWIFT_STRICT_CONCURRENCY: complete
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/OpenClawCN.app/OpenClawCN"
        BUNDLE_LOADER: "$(TEST_HOST)"
    info:
      path: Tests/Info.plist
      properties:
        CFBundleDisplayName: OpenClawCNTests
        CFBundleShortVersionString: "2026.2.15"
        CFBundleVersion: "20260215"
